version: '3.3'

services:
  database:
    image: custom-mariadb
    container_name: userService.database
    ports:
      - 3306:3306
    environment:
      - MARIADB_ALLOW_EMPTY_ROOT_PASSWORD=y
      - MARIADB_DATABASE=userService
    volumes:
      - ./bin:/outer
